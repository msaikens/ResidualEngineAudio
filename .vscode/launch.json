{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "ResidualAudioEngine (LLDB)",
        "type": "lldb",
        "request": "launch",
        "program": "${workspaceFolder}/build/voice_engine.exe",
        "args": [],
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "CMake: build"
      }
    ]
  }
  